#define TILE_WIDTH 256
#define Cur_pop_chrom(i) (current_pop+i*CHROM_MAX)
__kernel
        void get_parent(const int max_chrom)
        {
        //    char potParA[max_chrom]; //potential parent A
          //  char potParB[max_chrom];
            ///*copy_chrom(potParA, Cur_pop_chrom(rnd_member()));
            //copy_chrom(potParB, Cur_pop_chrom(rnd_member()));

            //if (fit_fun(potParA) < fit_fun(potParB))

        }


